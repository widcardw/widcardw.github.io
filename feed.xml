<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>widcardw</title>
        <link>https://widcard.win/</link>
        <description>widcardw's blog</description>
        <lastBuildDate>Tue, 31 Jan 2023 03:19:30 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <image>
            <title>widcardw</title>
            <url>https://widcard.win/avatar/avatar-round.png</url>
            <link>https://widcard.win/</link>
        </image>
        <atom:link href="https://widcard.win/feed.xml" rel="self" type="application/rss+xml"/>
        <item>
            <title><![CDATA[尝试编写一个编译器]]></title>
            <link>https://widcard.win/posts/notes/write-a-parser</link>
            <guid>https://widcard.win/posts/notes/write-a-parser</guid>
            <pubDate>Mon, 30 Jan 2023 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>尝试编写一个编译器</h1>
<h2>起因</h2>
<p>Manim 群友 Fran 做了一个 <a href="https://github.com/zmx0142857/qq-tex-bot">QQ tex bot</a> 并引入了群聊，看起来挺好用的，只需要满足匹配规则的消息就能生成公式的图片。它的依赖是 Fran 自改的 asciimathml 工具，能够使用比较简单的 ASCII 字符来表达比较复杂的公式。</p>
<p>这个原项目其实应该说年代有点久远了，而且源码与 DOM 是强耦合的，导致在 Node.js 环境下会有很多潜在的问题，而且也没有类型提示，所以我就有了重构它的想法。</p>
<p>我想做的其实就只是把 asciimath 格式的字符串转化为 $\LaTeX$ 格式的字符串，并不想去把 asciimathml 这种带标签的字符串转换为 $\LaTeX$ 的 DOM 元素了，毕竟现在也有 $\KaTeX$ 和 MathJax 这些工具，也就没有必要再去做这个工作了。</p>
<p>同时，前面也说到，旧代码与 DOM 耦合度非常高，因此，我想要做的，就是纯字符串处理的一个程序，而基本上完全与 DOM 隔离开。</p>
<h2>项目编写流程</h2>
<h3>Tokenize</h3>
<p>先看一个例子，下面的数学公式应该被渲染为 <code>$int_0^(+oo) &quot;e&quot;^(-x) dx = 1$</code></p>
<pre><code class="language-text">int_0^(+oo) &quot;e&quot;^(-x) dx = 1
</code></pre>
<p>我们发现，这个公式包含了非常多的关键字，以及一些可以直接作为 $\LaTeX$ 代码字符串的部分，我们需要找出所有的关键字，把它们包装成一些指定的类型（例如普通关键字、带有一个参数的关键字、带有多个参数的关键字等等），形成一个 token 序列，然后把它们传给下一级做进一步处理。</p>
<p>然而，asciimath 中内置的关键字就有 200 多个，而且应当还能添加自定义关键字，如果读取不固定的字符串，与非常大的一个集合去匹配，这样做显然是不明智的。对于这种情况，我们也就理所应当地使用<mark>字典树</mark>来进行匹配了[^1]。</p>
<p>[^1]: 当然询问 Fran 之后，发现原本就是用字典树来匹配的</p>
<h4>构建字典树</h4>
<p>我们在算法中认识的字典树，通常每个节点内字符元素的长度是 26，因为我们常用的也就是 26 个英文字母。而在这里，关键字可能包含大小写、一些特殊符号等，所以还必须向节点中额外添加一些元素，以满足当前的要求。</p>
<p>然而，这样又会让由字符找到数组索引较为困难。由于 Ascii 字符也不是很多，因此直接用一个 <code>Map</code> 映射就可以了。当然，应该还有更好的解决方案<s>只是现在代码已经写完了，我也懒得改了</s>。</p>
<details class="custom-callout tip" open><summary class="callout-title"><div class="callout-icon"></div><div>JavaScript 中带有 emoji 的字符串</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>Fran 在阅读我的代码的时候，发现我使用了 C 语言的方式来遍历字符串，并没有考虑到超过 <code>\uffff</code> 的字符，因此建议使用 <code>[...str].forEach</code> 这种方式来遍历字符串。<s>虽然我并没有用 emoji 作为关键字的打算</s></p>
</div>
</details><h4>搜索关键字</h4>
<p>其实这棵字典树承担了 Tokenize 的所有工作，包括识别关键字、数字、其他符号等。另外受 markdown 启发，用一个空行来表示换行，因此也就用这种方式来作为 align 环境下的换行符了。</p>
<p>这样我们就有了这样的优先级排序</p>
<details class="custom-callout note" open><summary class="callout-title"><div class="callout-icon"></div><div>空行 <code>$&gt;-$</code> 跳过空白 <code>$&gt;-$</code> 关键字 <code>$&gt;-$</code> 数字 <code>$&gt;-$</code> 正体文字 (<code>\text</code> 环境) <code>$&gt;-$</code> 任意字符</div>
</summary></details><p>与此同时，将这些 token 分类，得到一个序列，而后传给下一步骤。</p>
<details class="custom-callout faq" open><summary class="callout-title"><div class="callout-icon"></div><div>碎碎念</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>具体分类详见 <a href="https://github.com/widcardw/asciimath-parser/blob/main/src/symbols.ts#L1">https://github.com/widcardw/asciimath-parser/blob/main/src/symbols.ts#L1</a></p>
<p>其实我是没想到竟然需要分这么多类，但是 asciimath 确实有这么多种分类，根据这么多分类，才能在 Parse 阶段转换为合理的 AST ¯\_(ツ)_/¯</p>
</div>
</details><h3>Parse</h3>
<p>对于一个扁平的 token 序列，我们肯定需要使用<mark>递归</mark>才能创建一个树形的结构，而整个程序的流程大概就是这样</p>
<pre><code class="language-ts">function parser(tokens: TokenizedValue[]) {
  const root = createRootNode()
  let current = 0
  while (current &lt; tokens.length) {
    const res = walk(tokens, current) // 读取并处理一个节点
    root.body.push(res.node)          // 将读取到的节点加入序列中
    current = res.current             // 更新当前指针
  }
  return root
}
</code></pre>
<p>而最关键的逻辑就在 <code>walk</code> 中了。</p>
<h4>简单的 Token 处理方法</h4>
<p>对于数字、正体文本、可以单独使用的符号，可以直接简单的创建对应的节点即可，不需要递归处理，这其实也就相当于是递归的一部分终止情况。</p>
<h4>被左右括号包裹的情况</h4>
<p>我们将 <code>[1,2,3]</code> 这种仅仅包含逗号的序列，直接作为普通的数组来看，因此事实上可以直接依次返回，但我们还是将它包裹为一个节点的序列，将整个节点序列封装为一个新的节点而后返回。</p>
<details class="custom-callout faq" open><summary class="callout-title"><div class="callout-icon"></div><div>为什么需要这样处理</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>从最开始的例子中，我们发现 <code>(-x)</code> 也是被包裹在一对括号中的，整个节点都被当作一个上标作为了 <code>^</code> 的参数，因此还是有必要这样处理的。</p>
</div>
</details><p>如果这个被括号包裹的序列中出现了分号，那么我们就需要将它处理为矩阵。例如 <code>[1,2;3,4]</code></p>
<pre><code class="language-am">[1,2;3,4]
</code></pre>
<p>这样其实逻辑就很明了了，只需要逐行地去构建矩阵就行了。</p>
<p>然而，我在 <a href="http://asciimath.org">asciimath.org</a> 看到了另外的一个用法，我也想把它复刻过来</p>
<pre><code class="language-am">[1,2|3;4,5|6]
</code></pre>
<pre><code class="language-text">[1, 2 | 3; 4, 5 | 6]
</code></pre>
<p>思路是，在 <code>matrix</code> 环境内再嵌套一个 <code>array</code> 环境，使用 <code>{cc|c}</code> 参数来指定分割线的位置。为了简单化处理，在扫描矩阵的每一行时，每当遇到 <code>|</code> 时，都记录下当前竖线所在矩阵的位置，最后汇总所有竖线应当出现的列数，得到对应的 <code>{cc|c}</code> 参数即可。</p>
<p>综上，这一段程序需要不断向后查找，直到找到匹配的右括号，还需要判断在这一段 token 序列中是否出现了分号，以此来判断要将它处理为数组、矩阵还是其他类型。</p>
<h4>被竖线包裹的表达式</h4>
<p>竖线作为一个非常特殊的存在，可以作为绝对值两侧的包裹，也可以作为矩阵行列式的左右包裹，还能单独使用，因此也是需要像上面矩阵中说到的那样预先判断，而后才能进一步处理。</p>
<p>有以下这些情况，分别处理即可。</p>
<ul>
<li>单独使用 <code>${(x,y)|x^2+y^2&lt;=1}$</code></li>
<li>绝对值 <code>$|ln x|$</code></li>
<li>行列式 <code>$|1,2;3,4|$</code></li>
</ul>
<h4>除法</h4>
<p>我们希望用一个 <code>/</code> 符号就能表示除法，并将这个表达式转换为分式 <code>frac</code> 的形式，那么就不得不在读取完当前节点 $A$ 后，立即向后观察是否存在除法。如果存在，那么还需要进一步读取，得到分母 $B$ 后，将它渲染为 <code>\frac{A}{B}</code> 的形式。于是，<code>walk</code> 程序应当这样安排。</p>
<pre><code class="language-ts">function walk(tokens: TokenizedValue[], current: number) {
  // 将当前的 token 处理为 ast 的节点，并使指针前进
  const token = tokens[current]
  let { node, cur } = processCurrentToken(token)
  current = cur
  if (current &lt; tokens.length) {
    const nextToken = tokens[current]
    // 检测下一个 token 是否为除法
    if (nextToken.type === TokenTypes.Div) {
      // 创建一个双参数节点
      const newNode = createNodeWithTwoParams('\\frac')
      current++
      // 读取分母
      const b = walk(tokens, current)
      current = b.current
      // 分子
      newNode.param[0] = node
      // 分母
      newNode.param[1] = b.node
      node = newNode
    }
  }
  return { node, current }
}
</code></pre>
<p>当然，有一个优先级也比较高的运算符——阶乘，只不过它不需要继续向后读取了，用更简单的方式处理即可。</p>
<h4>带有参数的表达式</h4>
<h5>上下标</h5>
<p>上下标应当是优先级最高的，比除法的优先级更高，应当把 <code>pi^2/6</code> 渲染为 <code>$pi^2/6$</code> 而不是 <code>$pi^(2/6)$</code></p>
<p>基于这个例子，我们进行这样的分析：在读取完当前的 token 之后，继续向后看一个 token，如果是上下标，那么需要继续向后读取，直到组成一个符合语法的上下标为止。但是考虑到一些优先级的关系，又有下面的情况</p>
<ul>
<li>对于 <code>pi^2</code> 这样简单的式子，无须考虑优先级</li>
<li>对于 <code>pi^2/6</code> 带有除法的，读取到上标为止就直接结束，将 <code>pi^2</code> 打包为一个节点，再处理除法</li>
<li>对于 <code>a_1^2</code> 既有下标又有上标的，读取到一个下标就结束，将 <code>a_1</code> 打包为一个节点，再处理上标</li>
</ul>
<p>这样，只需在 look forward 阶段加一个循环，不断将第一个合法的节点包装起来，进入下一个循环继续读取即可。</p>
<h5>其他带有一个或两个参数的表达式</h5>
<p>例如 <code>sqrt</code>, <code>abs</code>, <code>frac</code> 这类的表达式，它们只需向后读取一个或两个节点，即可依据这些节点，生成一个新的表达式节点，似乎反而不需要考虑优先级了。</p>
<h3>Code Generation</h3>
<p>在上一步中，我们已经构建好了 AST 树，在代码生成阶段，只需深度优先遍历这棵树即可。上一步生成的节点类型有以下几类，由于类型较少，所以处理起来就相当方便了。</p>
<pre><code class="language-ts">enum NodeTypes {
  Root = 'Root',
  Const = 'Const',       // 可直接生成目标代码的节点
  ParamOne = 'ParamOne', // 带有一个参数的节点
  ParamTwo = 'ParamTwo', // 带有两个参数的节点
  Matrix = 'Matrix',     // 矩阵
  Flat = 'Flat',         // 包裹了一系列节点的节点
}
</code></pre>
<p>由于是深度优先遍历，那么就不可避免的需要用到递归。</p>
<h3>Const 节点</h3>
<p>可以直接查询该节点对应的 tex 代码输出即可。</p>
<h3>根节点 &amp; Flat 节点</h3>
<p>由于根节点其实跟 Flat 节点没有本质的区别，因此可以用类似的方式处理：直接遍历节点内包裹的系列节点并生成对应的代码即可。</p>
<details class="custom-callout tip" open><summary class="callout-title"><div class="callout-icon"></div><div>一些处理上的区别</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>由于修改版的 asciimath 支持 aligned 环境，因此还需要检测根节点中是否存在 aligned 环境相关的节点。若存在，那么就包裹上 aligned 环境即可。</p>
</div>
</details><h3>矩阵节点</h3>
<p>由于矩阵节点在 parse 阶段被处理成了二维数组，只需遍历就可以翻译成扁平的 $\LaTeX$ 代码了。</p>
<h3>带有参数的节点</h3>
<p>由于 <a href="https://github.com/widcardw/asciimath-parser/blob/main/src/symbols.ts#L214"><code>/ico i-vscode-icons-file-type-typescript-official;symbols.ts</code></a> 的 <code>tex</code> 关键字中，我使用了 <code>$1</code> 和 <code>$2</code> 这样的标识符来占位，因此在代码生成阶段，可以直接使用字符串替换即可传递参数。</p>
<h2>完整代码</h2>
<p><a href="https://github.com/widcardw/asciimath-parser">https://github.com/widcardw/asciimath-parser</a></p>
<h2>参考资料</h2>
<ul>
<li>Fran 自改的 <a href="https://github.com/zmx0142857/asciimathml">asciimathml</a> 以及<a href="https://zmx0142857.github.io/note">在线公式编辑器</a></li>
<li>Asciimathml <a href="https://github.com/asciimath/asciimathml">原项目</a>及其<a href="http://asciimath.org">官网</a></li>
<li><a href="https://space.bilibili.com/175301983/channel/collectiondetail?sid=654698">手写一个编译器</a> - 阿崔 cxr</li>
</ul>
]]></content:encoded>
            <author>widcardw@foxmail.com (widcardw)</author>
        </item>
        <item>
            <title><![CDATA[编写自己的动画]]></title>
            <link>https://widcard.win/posts/manim/create-your-own-animation</link>
            <guid>https://widcard.win/posts/manim/create-your-own-animation</guid>
            <pubDate>Tue, 03 Jan 2023 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>编写自己的动画</h1>
<h2>前言</h2>
<p>在前面几章的教程中，我们学习了 manim 是如何处理和生成动画的，也学习了 updater 该怎么编写。既然我们也了解了动画，那么也该开始造一点轮子了。</p>
<h2>Animation 与 Scene</h2>
<p>之前的文章中，我们也稍微提到了一些 Animation 和 Scene 的细节，我们再来复习一下。</p>
<p><img src="https://widcard.win/updater/animation-scene-play.excalidraw.svg" alt="updater/animation-scene-play.excalidraw.svg"></p>
<h3>准备工作[^1]</h3>
<p>[^1]: 这部分其实只有 Scene 在做，Animation 基本上并不参与</p>
<p>将写在一个 <code>play</code> 当中的所有动画抽取出来，将没有实例化的动画都实例化[^2]。当然，有些动画的参数被写在了 <code>play</code> 中，而没有写入动画的实例中，因此也需要分别注入。</p>
<p>[^2]: 自 Manim Community 提出 PR，增加了 <code>.animate</code> 属性之后，可以使用 <code>mobject.animate.shift(UP)</code> 这样的方法来更加方便的构建动画，但是这条语句并不会直接创建动画的实例，而是创建一个 <code>_AnimationBuilder</code>，因此需要在 <code>prepare</code> 阶段中实例化</p>
<h3>开启动画</h3>
<h4>Scene 的工作</h4>
<ul>
<li>调用该 <code>play</code> 中的所有 <code>animations</code> 的 <code>begin</code> 方法（即跳转到下面 Animation 的工作）</li>
<li>将不在场景中的 <code>mobject</code> 都添加到场景中</li>
</ul>
<h4>Animation 的工作</h4>
<p>因为动画应当是持续一段时间的，所以需要明确这段动画的起止时间点 <code>time_span</code></p>
<ul>
<li>这个 <code>time_span</code> 是可以人为指定的，例如整个 <code>play</code> 持续 3 秒，但是只需要某个动画在第 1 秒结束时开始，到第 2 秒结束时终止，那么 <code>time_span</code> 可以指定为 <code>(1, 2)</code> <s>不会吧不会吧不会还有人不知道有这个属性吧</s></li>
<li>一般地，我们不指定 <code>time_span</code> 时，默认的起止时间点就是 <em>当前时间点</em> 和 <em>加上 <code>run_time</code> 之后的时间点</em></li>
</ul>
<p>如果上一步我们指定了 <code>time_span</code>，也就是说在动画的 3 秒过程中，只有 1 秒的时间是给这个动画的，那么该怎么实现呢？答案是用 <code>rate_func</code>.</p>
<p><img src="/updater/squish-rate-func.excalidraw.svg" alt="updater/squish-rate-func.excalidraw.svg"></p>
<p>我们将 <code>smooth</code> 函数变换到右边的时间轴上，缺失值分别用 <code>start</code> 和 <code>end</code> 所对应的值代替。之后的 <code>rate_func</code> 就使用这个变换后的了。</p>
<p>接下来是设置当前物件的状态为<mark>正在运行动画</mark>，相当于是一个互斥锁了，后面好像只是在 shader 渲染的部分用到了一下。<s>这部分的源码暂时还没看，OpenGL 的内容太多了</s></p>
<p>然后是创建一个物件的初始状态 <code>starting_mobject</code>，这是为了之后方便计算而生的。</p>
<p><code>suspend_mobject_updating</code> 这个参数指定了，在这个动画中，是否允许 <code>updater</code> 生效，当为 <code>True</code> 时，那么就不允许 <code>updater</code> 参与这段动画。</p>
<p><code>families</code> 成员其实我也没怎么看懂，似乎是用于嵌套的 <code>Mobject</code>，对于含有子物件的 <code>Mobject</code>，或者说是更广义一点的 <code>Group</code>。当遇到一个组合物件的动画时，那么就对每个子物件都执行这段动画。</p>
<p>最后，把物件的呈现状态设置为运动的开始，即 <code>anim.interpolate(0)</code></p>
<h3>动画的过程</h3>
<h4>Scene 所做的工作</h4>
<p>在 <code>progress_through_animations</code> 这个方法中，首先根据动画的时长，生成一段时间轴，能够刚好填入 <code>play</code> 中的所有动画实例。</p>
<p>我们知道，视频是由很多个<mark>帧</mark>组成的，那么动画生成的原理就很简单，对于这个时间片段中，每秒采样 $n$ 次，$n$ 即为帧率。计算出每一帧的画面，然后将得到的结果输出给图形接口即可。</p>
<pre><code class="language-python">def progress_through_animations(self, animations: Iterable[Animation]):
	last_t = 0
	# 对每一帧进行采样
	for t in self.get_animation_time_progression(animations):
		dt = t - last_t
		last_t = t
		# 动画的处理
		for animation in animations:
			animation.update_mobjects(dt)
			alpha = t / animation.run_time
			animation.interpolate(alpha)
		# 更新相机拍摄到的画面，以及刷新缓冲之类的操作
		self.update_frame(dt)
		# 输出给图形接口
		self.emit_frame()
</code></pre>
<h4>Animation 所做的工作</h4>
<p>上面的代码中，<code>animation</code> 做了两件事。第一个是 <code>update_mobjects</code>，它的作用是按照 <code>mobject</code> 包含的 <code>updater</code> 来更新，<code>updater</code> 也就是在这里完成了它的职能。</p>
<p>第二个是 <code>interpolate</code>，其实就是动画实际执行的部分了。在这一步中，<code>alpha</code> 经过 <code>rate_func</code> 的变换之后，传入了 <code>interpolate_mobject</code>，方便之后可能会用到对嵌套物件进行递归处理。其中调用的 <code>interpolate_submobject</code> 是空的，需要我们后续去实现。</p>
<pre><code class="language-python">def interpolate_submobject(
	self,
	submobject: Mobject,
	starting_submobject: Mobject,
	alpha: float
):
	# Typically ipmlemented by subclass
	pass
</code></pre>
<p>实现方法其实也很简单，就是根据已有的参数 <code>submobject</code>, <code>starting_submobject</code>, <code>alpha</code> 参数，以及可能会用到的成员变量，更改 <code>submobject</code> 这个对象，让它成为动画过程中对应时间点理应成为的状态。</p>
<details class="custom-callout note" open><summary class="callout-title"><div class="callout-icon"></div><div>Note</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>Grant 在这里巧妙地使用了 <code>*mobs</code> 将这个元组展开，简直就是黑魔法，在这里看上去是把一个<mark>二元组</mark>展开为 <code>submobject</code> 和 <code>starting_submobject</code> 两个元素，但是在函数重载之后，会有更加巧妙的用法。</p>
<pre><code class="language-python">def interpolate_mobject(self, alpha: float) -&gt; None:
    for i, mobs in enumerate(self.families):
       sub_alpha = self.get_sub_alpha(alpha, i, len(self.families))
       self.interpolate_submobject(*mobs, sub_alpha)
</code></pre>
</div>
</details><h3>动画的结束</h3>
<p>Scene 其实要做的事情很简单，就是调用每个 <code>animation</code> 的 <code>finish</code> 方法，并将可能需要移除的物件都从场景中移除。</p>
<p>Animation 的工作其实也不多，就是将 <code>alpha</code> 设置为结束时的值，并且解除正在运行动画的状态。如果前面有禁用 <code>updater</code> 的物件，那么就把它的 <code>updater</code> 恢复回来。</p>
<h2>浅析 Transform 的原理</h2>
<p>我们常用的 Transform 一般来说是这么用的（虽然用的更多的是 <code>ReplacementTransform</code>，但只是看动画原理的话差别不大）</p>
<pre><code class="language-python">class ExampleScene(Scene);
	def construct(self):
		# ...
		self.play(Transform(A, B))
</code></pre>
<h3>初始化</h3>
<p>这里高亮的语句中，其实创建了 <code>Transform</code> 动画的实例，也就是说隐含的调用了它的 <code>__init__</code> 方法，做了一些初始化。</p>
<ul>
<li>调用父类 <code>Animation</code> 的构造函数</li>
<li>将 <code>target_mobject</code> 加入到成员变量中，以便动画中操作</li>
<li>初始化 <code>path_func</code>，会根据 <code>path_arc</code> 参数的值来计算
<ul>
<li>如果不指定 <code>path_arc</code>，那么路径将会是一条直线</li>
<li>如果指定了，那么路径将会是圆心角为 <code>path_arc</code> 的圆弧</li>
</ul>
</li>
</ul>
<h3>动画开始</h3>
<p>首先是做一些检查、拷贝之类的准备工作，避免在某些函数调用中会更改本来不应该更改的成员属性。</p>
<pre><code class="language-python">self.mobject.align_data_and_family(self.target_copy)
</code></pre>
<p>这一行是为了 Transform 的核心——插值——而生的。我们在前面也学习到，所有的 <code>VMobject</code> 都是由许多贝塞尔曲线构成的，而这些贝塞尔曲线又少不了它的控制点。所以如果直接操作这些控制点（锚点），那么就可以直接改变呈现出的物件形状。</p>
<p>而 Transform 的本质，实际上也就是将两个物件对应的锚点<mark>一一匹配</mark>起来，然后按照给定的轨迹，从一端运动到另一端。<code>align_data_and_family</code> 这个方法就是让物件的初始状态和终止状态的锚点等属性<mark>相互匹配</mark>，以便<mark>每个配对</mark>都可以按照给定的规则来执行插值。</p>
<details class="custom-callout quote" open><summary class="callout-title"><div class="callout-icon"></div><div>Quote</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>在我之前的<a href="https://www.bilibili.com/video/BV1EA411g7tz">视频</a> 3:00 处提到的“对于锚点较少的那个图形的锚点进行插值”其实也不能说很准确，但其实也就是上面的意思。</p>
</div>
</details><p>在调用父类的 <code>begin</code> 函数之后，有这样一个操作</p>
<pre><code class="language-python">if not self.mobject.has_updaters:
	self.mobject.lock_matching_data(
		self.starting_mobject,
		self.target_copy,
	)
</code></pre>
<p>这个 <code>lock_matching_data</code> 是为了改善计算时的性能而存在的，避免在后面的 <code>interpolate</code> 中进行过多的重复运算。</p>
<h3>动画过程</h3>
<p>我们前面说到动画过程只需要关注 <code>interpolate_mobject</code> 和 <code>interpolate_submobject</code>，我们来看一下 Transform 对它的实现。</p>
<pre><code class="language-python">def interpolate_submobject(
	self,
	submob: Mobject,
	start: Mobject,
	target_copy: Mobject,
	alpha: float
):
	submob.interpolate(start, target_copy, alpha, self.path_func)
	return self
</code></pre>
<p>欸，我记得父类的这个方法接收的是 3 个参数啊，为什么在这里多出来了一个 <code>target_copy</code> 呢？实际上，这就是父类 <code>interpolate_mobject</code> 方法中 <code>*mobs</code> 这个展开式的“灵活应用”。</p>
<p>在 Transform 中，重载的不仅仅是插值函数，还有 <code>get_all_families_zipped</code>，这个函数就负责创建 <code>families</code> 成员。我们来看看它的重载实现。</p>
<pre><code class="language-python">def get_all_families_zipped(self) -&gt; zip[tuple[Mobject]]:
	return zip(*[
		mob.get_family()
		for mob in [
			self.mobject,
			self.starting_mobject,
			self.target_copy,
		]
	])
</code></pre>
<p>可以看到，其实这里的 <code>families</code> 就变成了<mark>三元组</mark>的列表了，而不是前面提到的<mark>二元组</mark>列表。这样，上面 <code>interpolate_submobject</code> 方法也不能说是 override，而是重新创建了一个新的同名不同形参的函数。</p>
<p>在这个方法里面，实际上还是调用了 <code>submob</code> 的 <code>interpolate</code> 方法，而它本质上还是直接修改 <code>submob</code> 的属性，达到那种补间的效果。</p>
<pre><code class="language-python">def interpolate(
	self,
	mobject1: Mobject,
	mobject2: Mobject,
	alpha: float,
	path_func: Callable[[np.ndarray, np.ndarray, float], np.ndarray]
):
	for key in self.data:
		# ... 一些性能优化和预处理 ...
		if key in (&quot;points&quot;, &quot;bounding_box&quot;):
			func = path_func
		else:
			func = interpolate

		self.data[key][:] = func(
			mobject1.data[key],
			mobject2.data[key],
			alpha
		)
	for key in self.uniforms:
		self.uniforms[key] = interpolate(
			mobject1.uniforms[key],
			mobject2.uniforms[key],
			alpha
		)
	return self
</code></pre>
<p>上面代码中高亮的部分，实际上就是直接修改 Mobject 的属性，是非常简单的 <code>interpolate</code>.</p>
<h3>动画的结束</h3>
<p>剩余的清扫工作其实非常简单，就是调用父类的 <code>finish</code> 方法，并解除 <code>locked_data</code>.</p>
<p>现在，如果读者现在回看我之前写到的 <a href="/posts/manim/c01_front_knowledge#how-write-works">Write 的工作原理</a>，应该就相当明了了吧。</p>
<h2>编写自定义的动画</h2>
<p>正篇终于开始了。</p>
<h3>旋转淡入</h3>
<p>如果有看过我做过的视频教程的读者，应该对 <a href="https://github.com/3b1b/manim/blob/669182944da040c227ccfd018250ee2bbc49649d/manimlib/mobject/mobject_update_utils.py#L83"><code>turn_animation_into_updater</code></a> 还有印象，但这次我们不用它，而使用纯写动画的方法。</p>
<p><video controls="controls" src="/updater/RotateFadeInExample.mp4" alt="updater/RotateFadeInExample.mp4" style="width: 100%; height: 100%;"></video></p>
<h4>初始化</h4>
<p>首先是它该如何构造，我们很轻松地就能写出它的 <code>__init__</code> 函数。</p>
<pre><code class="language-python">class RotateFadeIn(Animation):
    def __init__(
        self,
        mobject: Mobject,      # 需要淡入的物件
        angle: float = PI,     # 旋转的角度
        axis: np.ndarray = UP, # 绕哪个轴旋转
        **kwargs
    ):
        self._angle = angle
        self._axis = axis
        super().__init__(mobject, **kwargs)
</code></pre>
<details class="custom-callout caution" open><summary class="callout-title"><div class="callout-icon"></div><div>Caution</div>
<div class="callout-fold"></div></summary><div class="callout-body">
<p>至于为什么我不用 <code>CONFIG</code> 字典来写入参数，是因为在撰写这篇文章的时候，Grant 已经在一个 PR 中把 <code>CONFIG</code> 字典给扬了。为了避免各种版本之间的冲突，我就直接用成员变量了。</p>
</div>
</details><h4>begin</h4>
<p>想要物件最终旋转到正确的角度，那么必须在一开始，让初始状态向反方向旋转相应的角度，那么也就有了这样的实现。</p>
<pre><code class="language-python"># 使用 `rotate` 方法
def begin(self):
	self._cached_angle = 0
	super().begin()
	self.mobject.rotate(-self._angle, self._axis)
</code></pre>
<pre><code class="language-python"># 使用 `become` 方法
def begin(self):
	super().begin()
	self.starting_mobject.rotate(-self._angle, self._axis)
</code></pre>
<h4>interpolate</h4>
<p>在对 alpha 的每个采样时，都先让 <code>mobject</code> 恢复初态，这里我采用的是旋转回到原来的位置，当然，如果大家对 API 比较熟悉的话，可以直接用 <code>become</code> 这个方法。</p>
<pre><code class="language-python"># 使用 `rotate` 方法
def interpolate_mobject(self, alpha: float):
	self.mobject.rotate(-self._cached_angle, self._axis) # 向原来的方向旋转
	self._cached_angle = alpha * self._angle # 计算新的角度
	self.mobject.rotate(self._cached_angle, self._axis)
	self.mobject.set_opacity(alpha)
</code></pre>
<pre><code class="language-python"># 使用 `become` 方法
def interpolate_mobject(self, alpha: float):
	self.mobject.become(self.starting_mobject) # 用初始状态覆盖当前状态
	self.mobject.rotate(alpha * self._angle, self._axis) # 旋转对应角度
	self.mobject.set_opacity(alpha)
</code></pre>
<h4>finish</h4>
<p>其实甚至都不用写，因为 interpolate 最后一步已经基本上都做完了，交给父类去做就可以了。</p>
<h4>测试</h4>
<pre><code class="language-python">class RotateFadeInExample(Scene):
    def construct(self) -&gt; None:
        r = Text(&quot;Rotate&quot;, font=&quot;Jetbrains Mono&quot;, slant=ITALIC).scale(3)
        self.play(RotateFadeIn(r, run_time=3))
</code></pre>
<h3>一个复杂一点的例子</h3>
<p><video controls="controls" src="/updater/Aniplex.mp4" alt="updater/Aniplex.mp4" style="width: 100%; height: 100%;"></video></p>
<p>——这真的是用 manim 写出来的吗？</p>
<p>——是的，源码在 <a href="https://github.com/widcardw/my-manim-projects/blob/master/videos/aniplex.py">GitHub 仓库</a>。</p>
<p>因为我懒，所以感兴趣的读者直接看源码吧，当时也只是灵感乍现随便写的，各种参数调的还不是很好，如果之后有机会的话那我就把这个坑填上吧。</p>
]]></content:encoded>
            <author>widcardw@foxmail.com (widcardw)</author>
        </item>
    </channel>
</rss>